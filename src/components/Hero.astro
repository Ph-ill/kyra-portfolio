---
import { siteConfig } from "../config";
---

<div
  id="hero"
  class="relative isolate overflow-hidden bg-white py-24 md:h-screen"
  style={`--accent-color: ${siteConfig.accentColor}`}
  role="banner"
  aria-label="Introduction and profile section"
>
  <!-- Gradient overlay -->
  <div
    class="absolute inset-0 -z-10"
    style={`background: radial-gradient(ellipse 800px 1200px at 0% 0%, ${siteConfig.accentColor}40 0%, ${siteConfig.accentColor}25 20%, ${siteConfig.accentColor}10 40%, rgba(255, 255, 255, 0.3) 70%, rgba(255, 255, 255, 0.8) 90%, white 100%)`}
    aria-hidden="true"
  >
  </div>
  <svg
    aria-hidden="true"
    class="absolute inset-0 -z-10 size-full stroke-gray-200 [mask-image:radial-gradient(100%_100%_at_top_right,white,transparent)]"
    role="img"
    aria-label="Decorative programming symbols background pattern"
  >
    <defs>
      <pattern
        x="50%"
        y={-1}
        id="0787a7c5-978c-4f66-83c7-11c213f99cb7"
        width={200}
        height={200}
        patternUnits="userSpaceOnUse"
      >
        <path d="M.5 200V.5H200" fill="none"></path>
      </pattern>
      <pattern
        id="programming-symbols"
        x="0"
        y="0"
        width="400"
        height="400"
        patternUnits="userSpaceOnUse"
      >
        <text
          x="50"
          y="50"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="24"
          transform="rotate(-15)"
          aria-label="HTML closing tag symbol">&lt;/&gt;</text
        >
        <text
          x="150"
          y="100"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="20"
          transform="rotate(10)"
          aria-label="JavaScript object literal symbol">{}</text
        >
        <text
          x="250"
          y="80"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="18"
          transform="rotate(-5)"
          aria-label="Arrow function symbol">=&gt;</text
        >
        <text
          x="100"
          y="200"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="22"
          transform="rotate(15)"
          aria-label="Array bracket symbol">[]</text
        >
        <text
          x="300"
          y="180"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="20"
          transform="rotate(-10)"
          aria-label="Generic type symbol">&lt;&gt;</text
        >
        <text
          x="200"
          y="250"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="24"
          transform="rotate(5)"
          aria-label="Function parentheses symbol">()</text
        >
        <text
          x="50"
          y="320"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="18"
          transform="rotate(-8)"
          aria-label="Scope resolution symbol">::</text
        >
        <text
          x="350"
          y="300"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="22"
          transform="rotate(12)"
          aria-label="Equality operator symbol">==</text
        >
        <text
          x="150"
          y="350"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="20"
          transform="rotate(-15)"
          aria-label="Increment operator symbol">++</text
        >
        <text
          x="250"
          y="370"
          fill={siteConfig.accentColor}
          font-family="monospace"
          font-size="24"
          transform="rotate(8)"
          aria-label="Statement terminator symbol">;</text
        >
      </pattern>
    </defs>
    <rect
      fill="url(#programming-symbols)"
      width="100%"
      height="100%"
      opacity="0.2"
      aria-hidden="true"></rect>
    <rect
      fill="url(#0787a7c5-978c-4f66-83c7-11c213f99cb7)"
      width="100%"
      height="100%"
      stroke-width={0}
      aria-hidden="true"></rect>
  </svg>
  <div class="h-full mx-auto p-8 sm:p-12 md:p-24 flex items-center">
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 lg:gap-12 items-center w-full">
      <!-- Text Content -->
      <div class="order-2 lg:order-1 lg:col-span-2 min-w-0">
        <h2
          class="text-pretty text-xl sm:text-2xl md:text-5xl font-bold tracking-tight text-gray-700 animate-fade-in"
          id="greeting"
        >
          Hello! 👋
        </h2>
        <h1
          class="mt-6 sm:mt-8 md:mt-10 text-4xl sm:text-5xl md:text-6xl lg:text-8xl font-bold tracking-tight text-gray-800 animate-fade-in animation-delay-200"
          id="name-title"
        >
          I'm <span style={{ color: siteConfig.accentColor }}
            >{siteConfig.name}</span
          >
        </h1>
        <p
          class="mt-4 sm:mt-6 md:mt-8 text-pretty text-base sm:text-lg md:text-xl/8 font-medium text-gray-600 animate-fade-in animation-delay-400"
          id="job-title"
        >
          {siteConfig.title}
        </p>
        {siteConfig.location && (
          <p
            class="mt-2 sm:mt-4 sm:mt-6 text-pretty text-sm sm:text-base md:text-lg text-gray-500 animate-fade-in animation-delay-500"
            id="location"
          >
            📍 {siteConfig.location}
          </p>
        )}
      </div>

      <!-- Profile Picture -->
      <div class="order-1 lg:order-2 flex justify-center lg:justify-end">
        <div class="relative">
          <div class="w-48 h-48 sm:w-64 sm:h-64 md:w-80 md:h-80 lg:w-96 lg:h-96 rounded-full overflow-hidden shadow-2xl border-4 border-white animate-fade-in animation-delay-300">
            <img
              src="/profile-photo.jpg"
              alt={`${siteConfig.name}, a ${siteConfig.title} based in ${siteConfig.location || 'the United States'}. Professional headshot showing a confident control systems engineer.`}
              class="w-full h-full object-cover"
              loading="eager"
              width="384"
              height="384"
            />
          </div>
          <!-- Decorative ring -->
          <div 
            class="absolute inset-0 rounded-full border-4 border-transparent"
            style={`background: linear-gradient(45deg, ${siteConfig.accentColor}40, ${siteConfig.accentColor}20, ${siteConfig.accentColor}40)`}
            aria-hidden="true"
          ></div>
          
          <!-- Thought Bubble -->
          <div 
            class="absolute -top-12 -right-20 sm:-top-8 sm:-right-12 md:-top-12 md:-right-16 lg:-top-16 lg:-right-20 animate-fade-in animation-delay-700 scale-75 sm:scale-90 md:scale-100 pointer-events-none"
            role="img"
            aria-label="Animated thought bubble containing the text 'Thinking in ladder logic' with a ladder emoji, representing the engineer's mindset"
          >
            <!-- Main cloud-shaped bubble -->
            <div class="relative bg-white rounded-[30px] px-5 py-4 sm:px-5 sm:py-4 md:px-6 md:py-5 shadow-lg border-2 border-gray-200 animate-float-slow">
              <p class="text-base sm:text-sm md:text-base font-medium text-gray-700 whitespace-nowrap relative z-10">
                Thinking in ladder logic 🪜
              </p>
            </div>
            
            <!-- Medium bubble -->
            <div class="absolute -bottom-6 left-2/3 sm:left-1/2 w-4 h-4 sm:w-4 sm:h-4 bg-white rounded-full border border-gray-200 shadow-md animate-float-slow animation-delay-1" aria-hidden="true"></div>
            
            <!-- Small bubble -->
            <div class="absolute -bottom-8 left-3/5 sm:left-2/5 w-2.5 h-2.5 sm:w-3 sm:h-3 bg-white rounded-full border border-gray-200 shadow-sm animate-float-slow animation-delay-2" aria-hidden="true"></div>
            
            <!-- Tiny bubble -->
            <div class="absolute -bottom-10 left-1/2 sm:left-1/3 w-1 h-1 sm:w-2 sm:h-2 bg-white rounded-full border border-gray-200 animate-float-slow animation-delay-3" aria-hidden="true"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div
    class="absolute bottom-0 left-0 right-0 p-8 sm:p-12 md:p-24 flex gap-x-4 sm:gap-x-6 md:gap-x-8 text-gray-700 animate-fade-in animation-delay-600"
    role="navigation"
    aria-label="Social media and contact links"
  >
    {
      siteConfig.social?.email && (
        <a
          href={`mailto:${siteConfig.social.email}`}
          aria-label={`Send email to ${siteConfig.name} at ${siteConfig.social.email}`}
          class="transition-colors duration-300 hover:text-[var(--accent-color)]"
          title="Email"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8"
            role="img"
            aria-label="Email icon"
          >
            <path stroke="none" d="M0 0h24v24H0z" fill="none" />
            <path d="M3 7a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v10a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-10z" />
            <path d="M3 7l9 6l9 -6" />
          </svg>
        </a>
      )
    }
    {
      siteConfig.social?.linkedin && (
        <a
          href={siteConfig.social.linkedin}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={`Visit ${siteConfig.name}'s LinkedIn profile`}
          class="transition-colors duration-300 hover:text-[var(--accent-color)]"
          title="LinkedIn Profile"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8"
            role="img"
            aria-label="LinkedIn icon"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M8 11v5" />
              <path d="M8 8v.01" />
              <path d="M12 16v-5" />
              <path d="M16 16v-3a2 2 0 1 0 -4 0" />
              <path d="M3 7a4 4 0 0 1 4 -4h10a4 4 0 0 1 4 4v10a4 4 0 0 1 -4 4h-10a4 4 0 0 1 -4 -4z" />
            </>
          </svg>
        </a>
      )
    }
    {
      siteConfig.social?.instagram && (
        <a
          href={`https://instagram.com/${siteConfig.social.instagram.replace('@', '')}`}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={`Visit ${siteConfig.name}'s Instagram profile`}
          class="transition-colors duration-300 hover:text-[var(--accent-color)]"
          title="Instagram Profile"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8"
            role="img"
            aria-label="Instagram icon"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M7 2h10a5 5 0 0 1 5 5v10a5 5 0 0 1 -5 5h-10a5 5 0 0 1 -5 -5v-10a5 5 0 0 1 5 -5z" />
              <path d="M16 11.37a4 4 0 1 1 -7.914 1.173a4 4 0 0 1 7.914 -1.173z" />
              <path d="M17.5 6.5h.01" />
            </>
          </svg>
        </a>
      )
    }
    {
      siteConfig.social?.github && (
        <a
          href={siteConfig.social.github}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={`Visit ${siteConfig.name}'s GitHub profile to see code repositories and projects`}
          class="transition-colors duration-300 hover:text-[var(--accent-color)]"
          title="GitHub Profile"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8"
            role="img"
            aria-label="GitHub icon"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6c-.6 .6 -.6 1.2 -.5 2v3.5" />
            </>
          </svg>
        </a>
      )
    }
    <a
      href="https://blog.coolphill.com/phill/"
      target="_blank"
      rel="noopener noreferrer"
      aria-label={`Visit ${siteConfig.name}'s blog`}
      class="transition-colors duration-300 hover:text-[var(--accent-color)]"
      title="Blog"
    >
      <svg
        xmlns="http://www.w3.org/2000/svg"
        width="24"
        height="24"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
        class="h-6 w-6 sm:h-7 sm:w-7 md:h-8 md:w-8"
        role="img"
        aria-label="Blog icon"
      >
        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
        <path d="M4 4h6v6h-6z" />
        <path d="M4 14h6v6h-6z" />
        <path d="M14 4h6v4h-6z" />
        <path d="M14 12h6v8h-6z" />
      </svg>
    </a>
  </div>
</div>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fadeIn 0.8s ease-out forwards;
    opacity: 0;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
  }

  .animation-delay-300 {
    animation-delay: 0.3s;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
  }

  .animation-delay-600 {
    animation-delay: 0.6s;
  }

  .animation-delay-500 {
    animation-delay: 0.5s;
  }

  .animation-delay-700 {
    animation-delay: 0.7s;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  .animate-float {
    animation: float 3s ease-in-out infinite;
  }

  @keyframes floatSlow {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-8px);
    }
  }

  .animate-float-slow {
    animation: floatSlow 4s ease-in-out infinite;
  }

  .animation-delay-1 {
    animation-delay: 0.5s;
  }

  .animation-delay-2 {
    animation-delay: 1s;
  }

  .animation-delay-3 {
    animation-delay: 1.5s;
  }
</style>
